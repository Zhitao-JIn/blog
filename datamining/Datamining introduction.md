# Datamining introduction

## Datamining tasks

### Classification:

number recognize,

diagnosis detection

cheat detection

news aggregation: if we have exact class we use classification.

atlas-based segmentation: classify the images by the labels, which produced by expert.

### Outlier Detection:

cheat detection:

cheat gamer detection. it's not only tell if he is cheated or not, but analysis the feature of points. 99% of gamers are normal, so the points are not  balance, and also the way the cheater use is hard to be predicted, it's not good for classifier. but if a cheat way is known, we can use classification.

Shape-Based segmentation:  can't understand.

### Market basket analysis,association rule:

recommendation system

### Clustering

news aggregation: if some news mention same thing,we use cluster to gather them .

interactive Segmentation: temporary label some area to refine the results.

